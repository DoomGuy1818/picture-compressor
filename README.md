# picture-compressor
This is small backend for compressing picture with using:
  -minio/S3 as object vault
  -kafka as event sender-receiver unit
  -postgres as event saver for implementation transactional outbox pattern
